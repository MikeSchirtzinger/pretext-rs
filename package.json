{
  "name": "pretext-rs",
  "version": "0.2.0",
  "private": true,
  "description": "Browser demo and parity benchmark for pretext-rs",
  "scripts": {
    "build:wasm": "wasm-pack build --release --target web -- --features wasm",
    "test:rust": "cargo test --all-targets --all-features"
  },
  "keywords": [
    "text-layout",
    "wasm",
    "rust"
  ],
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@chenglou/pretext": "^0.0.8"
  }
}
